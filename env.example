# FlowGuard Docker Compose Environment Variables
# Copy this file to .env and update values as needed

# ============================================
# Backend Configuration
# ============================================
BACKEND_PORT=3001
NODE_ENV=production

# ============================================
# Database Configuration
# ============================================
# SQLite database path (persistent volume in Docker: /app/data/flowguard.db)
DATABASE_PATH=/app/data/flowguard.db

# ============================================
# Bitcoin Cash Network Configuration
# ============================================
# Options: mainnet, testnet3, testnet4, chipnet
BCH_NETWORK=chipnet

# BCH Node RPC Configuration
BCH_RPC_URL=http://bch-node:8332
BCH_RPC_USER=user
BCH_RPC_PASSWORD=pass
BCH_RPC_PORT=8332
BCH_P2P_PORT=8333

# ============================================
# Frontend Configuration
# ============================================
FRONTEND_PORT=80
# API URL for frontend (should point to backend service)
# In Docker, use service name; externally, use host URL
VITE_API_URL=http://localhost:3001/api
VITE_BCH_NETWORK=chipnet


# ============================================
# Security Notes
# ============================================
# 1. Change all default passwords in production
# 2. Use Docker secrets for sensitive data in production
# 3. Set strong BCH_RPC_PASSWORD (if using local BCH node)
# 4. Consider using environment-specific .env files

